# Week168（旧 habiterm 再構築）— 実装フェーズの指示と指摘

日付: 2026-09-14 〜 2026-09-16

## この記録について

**正本はここではない。** ユーザーの明示指示により、このプロダクトの記録一式は
`onclimb-industries/data/develop/habiterm/` に置いている（git 管理外）。

- `00-process/decisions.md` — 決定 D-001〜D-103 と原則 P-1〜P-15
- `00-process/questions-log.md` — 確認事項 Batch 1〜27
- `01-discovery/` 〜 `05-implementation/` — 各フェーズの成果物

**R-001 の趣旨（やり取りを会話の中で消費しない）はそちらで満たしている。**
ここには、my-own-apps リポジトリで作業するときに効く分だけを残す。

## ユーザーから出た実装方針・制約

| # | 指示 | 理由 | どこで効くか |
|---|---|---|---|
| 1 | **実装は Claude が直接 Codex に投げる。extremis は使わない** | 壊れていて無駄にトークンを使う | week168 の実装委譲すべて |
| 2 | 記録は `data/develop/habiterm/` に残す | git 管理外に置きたい | このプロダクトの全記録 |
| 3 | 暗黙的に対応してしまうところも、面倒臭いくらい確認して、記録は全て明示的に残す | 開発フローをエージェントの知識として残したい | 確認を飛ばしそうなときの判断基準 |
| 4 | タブは 5 つ。設定をタブから外す | 振り返りと設定が「その他」に埋まっていたため | `ContentView` |
| 5 | 削り候補（D-062 保存失敗の欠損ゼロ / D-063 VoiceOver 実機検証）は削らない | 期限に余裕があるため | T-14 / T-15 |

## Claude 側の運用で失敗して原則化したもの（P-9〜P-15）

**この 7 件は机上では出てこない。全部、実際に失敗してから出ている。**

| 原則 | 要旨 |
|---|---|
| P-9 | 破壊的な操作の前に、直前の操作の成否を「表示」ではなく「分岐」で確認する |
| P-10 | 変更範囲を切るときは、その変更が壊す隣接物も範囲に含める |
| P-11 | 失敗して終わったセッションを `--resume` しない |
| P-12 | 完了は、副作用が静かになったことではなく、完了の通知で判断する |
| P-13 | 層をまたぐ受け入れ条件は、最後の層で確認する（主語が「利用者」なら UI に届くまで未達） |
| P-14 | 画面を列挙したら、「どこから開くか」も列挙する |
| P-15 | 待機の終了条件は「終わった」で書く。「続いていない」で書かない |

## Codex 委譲で効いたこと

| # | 内容 |
|---|---|
| 1 | **指示書の冒頭に「このタスクが触る既存の決定」の表を貼る**（P-8）。前提が 1 往復で渡る |
| 2 | **パッケージに切ってシミュレータなしで `swift test` が回る**ようにする。Codex が自分で検証を完結できる |
| 3 | 「**仕様に矛盾や穴を見つけたら、実装せずに報告すること**」を毎回書く。<br>実際に T-10 / T-12 / T-13 で本物の穴が実装前に見つかった |
| 4 | 「**実行できないテストの期待値を、実装に合わせて書き換えない**」を明記する。<br>T-13 で未実行のまま書き換えが起きたため追加した |
| 5 | **完了報告に変更ファイル一覧を必ず出させ、`git status` と突き合わせる**（P-12） |

## skill 化の候補（R-002。未確認）

- **指示書に「触る既存の決定」の表を貼る**手順（上の 1）は、
  Codex 委譲のたびに効いている。`edith-issue-planner` に取り込む価値がある
- **完了報告と `git status` の突き合わせ**（上の 5）も同様

> **どちらもユーザーに確認していない。** 勝手に skill を増やさない（R-002）。
> week168 のリリース後、`knowledge/process/` への還元とまとめて相談する。
